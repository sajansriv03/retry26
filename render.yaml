services:
  - type: web
    name: wacky-west-online
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run server
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20
